public Boolean isTeenager(Integer age) {
    //code here
    boolean isTeenager;

    if (age >12 && age <20)
    {
        isTeenager = true;
        
    }
    else 
    {isTeenager = false;
    }
    return isTeenager;
}